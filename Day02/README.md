Day 02 - Login Attempt Analyzer

**Description:
Create a function login(username, password) that accepts arbitrary arguments using *args and **kwargs.
Raise a custom exception if the password is shorter than 8 characters.
Use a decorator to log every login attempt (successful or failed) to the console.
Skills Practiced:
Functions with *args/**kwargs, custom exceptions, and decorators.
